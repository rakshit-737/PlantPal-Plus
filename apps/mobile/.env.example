# API origin the app talks to.
#
# Defaults when unset: Android emulator -> http://10.0.2.2:4000 (the emulator's
# alias for the host machine), iOS simulator -> http://localhost:4000.
# A physical device needs your machine's LAN IP, e.g. http://192.168.1.20:4000.
EXPO_PUBLIC_API_URL=
